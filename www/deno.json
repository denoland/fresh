{
  "tasks": {
    "start": "deno run -A --allow-scripts --node-modules-dir --watch=static/,routes/,../src,../docs dev.ts",
    "build": "deno run -A dev.ts build",
    "preview": "deno run -A main.ts"
  }
}
